services:
  web:
    build: .
    container_name: pittsburghese-web
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.pittsburghese.rule=Host(`${DOMAIN}`)"
    networks:
      - traefik-net

networks:
  traefik-net:
    external: true
